"""Shared API route constants for tests.

Centralizes endpoint paths so route changes only require updates in one place.
"""

PREFIX = "/api/v1"

NARRATORS = f"{PREFIX}/narrators"
HADITHS = f"{PREFIX}/hadiths"
COLLECTIONS = f"{PREFIX}/collections"
SEARCH = f"{PREFIX}/search"
GRAPH = f"{PREFIX}/graph"
PARALLELS = f"{PREFIX}/parallels"
TIMELINE = f"{PREFIX}/timeline"
